import React from 'react';

const SignalBadge: React.FC = () => (
  <span style={{ background: '#4caf50', color: '#fff', borderRadius: 4, padding: '2px 8px' }}>
    Signal Badge
  </span>
);

export default SignalBadge;